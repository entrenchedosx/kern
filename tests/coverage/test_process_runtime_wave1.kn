code = exec_args(["cmd", "/c", "exit 0"])
assert_eq(code, 0)

h = spawn(["cmd", "/c", "ping 127.0.0.1 -n 2 >nul"])
st = wait_process(h, 10000)
assert_eq(has(st, "ok"), true)
assert_eq(has(st, "code"), true)

print("OK: test_process_runtime_wave1")
