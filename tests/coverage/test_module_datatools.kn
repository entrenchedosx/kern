m = import("datatools")
assert_eq(has(m, "json_parse"), true)
assert_eq(has(m, "toml_parse"), true)
assert_eq(has(m, "csv_parse"), true)
j = m.json_parse("{\"k\": 7}")
assert_eq(has(j, "k"), true)
rows = m.csv_parse("a,b\n1,2\n")
assert_eq(len(rows) >= 1, true)
print("OK: test_module_datatools")
