// Minimal entry for manual `kernc --config` smoke tests (see docs/STANDALONE_COMPILE_ARCHITECTURE.md).
// Run with `kern main.kn` from this directory, or use `kernc --config` from repo root (resolver uses entry's folder).
import "helper.kn"

print(fixture_greeting())
