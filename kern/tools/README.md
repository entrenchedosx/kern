# Kern Tools

This folder contains tooling scripts and build integration helpers for Kern.

- `../build.ps1` is the primary deterministic build + installer pipeline (optional).
- `../run_all_tests.ps1` runs repository test scripts.
- `../tools/vcpkg` contains local dependency management (raylib, etc.).
